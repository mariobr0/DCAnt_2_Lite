# Правила и настройки проекта DCAbot 2

> Данный файл автоматически загружается ассистентом Antigravity для проекта DCAbot_2_Lite.

---

## 1. Обязательные источники и приоритет правил

1. **Обязательные источники**:

C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\.agent\AGENTS.md
C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\openspec\config.yaml
C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\docs\ARCHITECTURE.md
C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\docs\PROJECT.md
C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\docs\references.md
C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\docs\ROADMAP.md

2. **Разрешение противоречий**:

- Если правила противоречат друг другу, не выбирай решение самостоятельно.

- Сообщи пользователю о противоречии простыми словами.

---

## 2. Основные руководящие принципы

- **Терминология валюты**: В пользовательской документации, UI, комментариях и названиях финансовых параметров запрещено использовать символ `$` как обозначение валюты. Для расчетной валюты используется точный код `USDT`. Использование `$` как синтаксического элемента языка C#, включая строковую интерполяцию (string interpolation), разрешено. Доменная модель использует структуру `Money` с явным указанием валюты.

- **Ведение журнала изменений**: Файл `docs/changelog_ru.md` обновляется для каждого завершенного изменения OpenSpec (OpenSpec change), релизного изменения, исправления пользовательского дефекта или изменения наблюдаемого поведения. Внутренние промежуточные коммиты без изменения поведения отдельной записи не требуют.

- **Запрет скрытого сокращения объема**: AI-агент и разработчик не имеют права молча сокращать согласованный объем работ (scope), пропускать критерии приемки (acceptance criteria), сценарии ошибок (failure scenarios), поведение при восстановлении (recovery behavior) или тесты. Любое упрощение должно быть явно предложено, обосновано и принято через изменение спецификации.

- **Проверка объема и символов перед отчетом**: Перед заявлением о завершении работы или объявлением готовности строго проверяй количество строк, символов и полноту содержимого, чтобы ни одно слово, правило, формула или требование не были утеряны, упрощены, сокращены или обобщены.

- **Отчет об ошибках PowerShell**: Ты обязан явно сообщать пользователю обо всех ошибках, предупреждениях или сбоях, возникающих при выполнении команд в командной строке PowerShell (ненулевые коды выхода, вывод stderr). Строго запрещено скрывать такие ошибки; если что-то идет не так, ты должен заявить об этом напрямую.

- **Запрет скрытого додумывания**: Если вопрос, запрос, размышление или просьба пользователя содержит неоднозначность, кажется неполным или допускает несколько толкований, AI-агент строго не имеет права додумывать намерение самостоятельно. Агент обязан вежливо переспросить и уточнить смысл простыми словами, объяснить возможные варианты и дождаться подтверждения пользователя.

- **Кодировка UTF-8**: Все текстовые файлы в проекте используют кодировку UTF-8 (согласно `.editorconfig`). При использовании скриптов (например, PowerShell `Set-Content` или `Out-File`) для создания или изменения файлов, агент обязан **явно указывать кодировку UTF-8** (например, `-Encoding UTF8`), чтобы избежать повреждения кириллических символов Windows-кодировками по умолчанию.

- **Запрет многострочных команд PowerShell**: AI-агент не должен использовать многострочные скрипты при вызове команд PowerShell в один шаг (без явного разделения символом `;`), так как это часто приводит к молчаливому игнорированию части команд. Все команды должны выполняться строго последовательно, команда за командой, либо с явным разделением `;`.

- **Обязательная проверка компиляции**: После любого изменения исходного кода на C# (даже если это "одна строчка") ИИ-агент обязан самостоятельно запустить команду `dotnet build` и убедиться в отсутствии ошибок компиляции. Строго запрещено сообщать пользователю о готовности задачи, если код не компилируется или сборка не была проверена.

- **Контроль внешних зависимостей**: Агенту строго запрещено добавлять в проект новые сторонние NuGet-пакеты (за исключением встроенных библиотек .NET и Quantower API) без предварительного обоснования и явного согласия пользователя. DCA-бот требует высокой производительности, и использование сторонних библиотек должно быть сведено к минимуму.

- **Бережное отношение к комментариям**: При редактировании или рефакторинге существующих файлов ИИ-агент обязан сохранять все оригинальные комментарии разработчиков (особенно пояснения бизнес-логики на русском языке), даже если код вокруг них меняется. Запрещено молча удалять или переписывать комментарии, не относящиеся напрямую к текущей задаче.

---

## 3. Регламент выполнения команд OpenSpec

Перед выполнением любой команды OpenSpec выполни следующие действия:
1. Перейди в корень `DCAnt_2_Lite`.
2. Выполни `Get-Location`.
3. Проверь наличие `openspec/config.yaml`.
4. Не передавай имя изменения (change) команде `openspec update`.
5. Для обновления артефактов конкретного изменения (change) используй `/opsx-update`.
6. Для проверки конкретного изменения (change) используй `openspec validate <change-name>`.

---

## 4. Подключенные правила стиля и контроля

Всегда соблюдай правила стиля речи из: C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\.agent\rules\concise_speech.md
Всегда соблюдай правила контроля сложности из: C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\.agent\rules\complexity_control.md
Всегда соблюдай правила безопасных изменений из: C:\Users\dzam\Desktop\Code\QuantowerCode\DCAnt_2_Lite\.agent\rules\minimal-safe-change.md